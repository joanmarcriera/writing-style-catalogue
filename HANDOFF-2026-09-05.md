# Handoff — 2026-09-05

**What changed**: Added `CLAUDE.md` (layout/conventions, run & test commands, key files, common
tasks, gotchas, project state). README.md was reviewed and left unchanged — it already had a
complete usage header.

**Verified against actual repo**:
- Content directory counts (styles 25, prompt-templates 23, skills 26, personal-workflows 9,
  examples 12, patterns 9, checklists 10, rubrics 11, `use-cases/` empty) — all match.
- `EXPECTED_BUNDLE_COUNT = 12` in `scripts/bundle_contract.py` — matches.
- Test/validation claims in "Project State" against `PROJECT_STATE.md` — matches.

**Fixed**: CLAUDE.md's "Last maintenance" date was wrong (said 14 May 2026, but the latest commit
`14f3a67` is dated 1 Aug 2026 — the same release referenced in "Project State (as of 2026-08-01)").
Corrected to 1 Aug 2026.

**Left as noted, no change needed**: dormant-since-Aug-2026 status and the known gaps
(empty `use-cases/`, no style comparison matrix) are accurate as-is; no further documentation
needed for them.
